import * as React from "react";

interface CreateConferenceProps {}

const CreateConference: React.FunctionComponent<CreateConferenceProps> = (
  _
) => {
  return <h1>create conference page</h1>;
};

export default CreateConference;
